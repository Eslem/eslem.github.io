$(document).ready(function () {
    sflooSliderCircles.imgWrap = '.slider-wrap';
    sflooSliderCircles.pagination = '.slider-pagination';
    sflooSliderCircles.init();
    sflooSliderCircles.interval(5000);
});
